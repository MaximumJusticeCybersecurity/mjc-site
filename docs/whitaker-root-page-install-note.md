# Whitaker Root Page Install

Whitaker must be explicitly loaded by each non-Squarespace site.  The Squarespace Footer injection only affects the Squarespace site.

This change adds the Whitaker server-TTS bridge and widget runtime to `site/index.html` so Whitaker appears on `https://app.maximumjusticecybersecurity.com/`.
