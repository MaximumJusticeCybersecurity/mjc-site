const ENDPOINT='https://xpgwklwzptutykzjamuo.supabase.co/functions/v1/top-chef-vote';
function send(res,status,body){res.statusCode=status;res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','no-store, max-age=0');res.setHeader('X-Content-Type-Options','nosniff');return res.end(JSON.stringify(body));}
function allowed(req){const origin=String(req.headers?.origin||'');const a=new Set(['https://app.maximumjusticecybersecurity.com']);if(process.env.VERCEL_URL)a.add(`https://${process.env.VERCEL_URL}`);return a.has(origin);}
module.exports=async function handler(req,res){if(req.method!=='POST'){res.setHeader('Allow','POST');return send(res,405,{ok:false,code:'METHOD_NOT_ALLOWED'});}if(!allowed(req))return send(res,403,{ok:false,code:'ORIGIN_NOT_ALLOWED'});try{const body=typeof req.body==='string'?req.body:JSON.stringify(req.body||{});if(Buffer.byteLength(body,'utf8')>4096)return send(res,413,{ok:false,code:'REQUEST_TOO_LARGE'});const r=await fetch(ENDPOINT,{method:'POST',headers:{'Content-Type':'application/json','Origin':'https://app.maximumjusticecybersecurity.com'},body,signal:AbortSignal.timeout(5000)});const data=await r.json();return send(res,r.status,data);}catch{return send(res,503,{ok:false,code:'VOTE_NOT_RECORDED'});}};
